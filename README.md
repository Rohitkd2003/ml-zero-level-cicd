# Zero Level Machine Learning CI/CD

This project trains a very simple ML model that predicts whether a number is even or odd.

The CI/CD pipeline:
- Runs tests using GitHub Actions
- Trains the model
- Deploys a small GitHub Pages site if tests pass
